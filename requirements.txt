# FastAPI and ASGI server
fastapi==0.115.12
uvicorn==0.34.2
itsdangerous==2.1.2  # Required by Starlette for SessionMiddleware

# Pydantic and settings management (Pydantic v2 compatible)
pydantic==2.11.4
pydantic-settings==2.9.1

# Database: SQLAlchemy + PostgreSQL support
SQLAlchemy==2.0.40
# Choose either `psycopg2-binary` (sync) or `asyncpg` (async)
psycopg2-binary==2.9.6
# asyncpg==0.29.0  # Uncomment if using SQLAlchemy async with PostgreSQL

# Authentication and security
python-jose==3.4.0
bcrypt==4.3.0
passlib==1.7.4
email_validator==2.2.0

# File upload and parsing
python-multipart==0.0.20
PyPDF2==3.0.1
mammoth==1.9.0

# Environment variables
python-dotenv==1.1.0

# HTTP clients
httpx==0.24.1
aiohttp==3.9.3

# AI / LLM integrations
google-generativeai==0.8.5

# Supabase client
supabase==1.2.0
